/* Hello Academy — connection settings
 *
 * Leave this file as it is and both apps run on browser storage, as a demo.
 * Fill in the two lines below and they run on the school's real database.
 *
 * Find both values in your Supabase project under Settings → API.
 * The anon key is meant to be public: it grants nothing on its own, because
 * every table has row level security forced on. Never put the service key here.
 */
window.HELLO_CONFIG = {
  supabaseUrl: '',        // e.g. 'https://abcdefghijkl.supabase.co'
  supabaseAnonKey: ''     // e.g. 'eyJhbGciOiJIUzI1NiIsInR5cCI6...'
};
